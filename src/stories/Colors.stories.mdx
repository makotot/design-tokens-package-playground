import { Meta } from '@storybook/addon-docs'
import { palette } from './baseColors'

<Meta title="Tokens/Colors" />

# COLORS

<div
  style={{
    marginTop: '1rem',
    display: 'grid',
    gap: '1rem',
    color: '#444',
  }}>
  {palette().map((p, index) => {
    return (
      <table
        key={index}
        style={{
          tableLayout: 'fixed',
          width: '100%',
        }}>
        <thead>
          <tr>
            <th>CTI</th>
            <th>Hex</th>
            <th>Example</th>
          </tr>
        </thead>
        <tbody>
          {p.map((pColor) => {
            console.log(pColor)
            return (
              <tr
                key={pColor.value}
              >
                <td style={{fontWeight: 'bold', textTransform: 'capitalize'}}>
                  <code>{pColor.attributes.type}/{pColor.attributes.item}/{pColor.attributes.subitem}</code>
                </td>
                <td><code>{pColor.value}</code></td>
                <td>
                  <div
                    style={{
                      padding: '1rem',
                      backgroundColor: pColor.value,
                      textAlign: 'center',
                    }}>
                  </div>
                </td>
              </tr>
            )
          })}
        </tbody>
      </table>
    )
  })}
</div>
